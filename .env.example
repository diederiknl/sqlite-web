# SQLite Web Environment Configuration
# Copy this file to .env and update with your values

# Secret key for Flask session management
# If not set, a secure random key will be generated and saved to .secret_key
# Generate a secure key with: python -c "import secrets; print(secrets.token_hex(32))"
SQLITE_WEB_SECRET_KEY=

# Password for accessing the database browser
# If set here, you won't need to use the -P flag
# SQLITE_WEB_PASSWORD=
